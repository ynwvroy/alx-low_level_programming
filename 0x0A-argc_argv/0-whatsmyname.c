#include "main.h"
#include <stdio.h>

/**
 * main - It is supposed to print its name
 * @argc: count argument
 * @argv: vector argument
 *
 * Return: Zero always
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
